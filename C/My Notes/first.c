#include <stdio.h>

int main(int argc, char const *argv[])
{
    printf("Hello, World!");
    // printf does not create any line after the text. 

    /*
    /n => new line
    /t => tab
    Comment syntax is same as Java.
    */
    return 0;
}
